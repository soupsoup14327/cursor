## Context / Style tokens

This folder contains link-free “context” data for future rebuilds:

- `context.json` lists CSS variables (design tokens), component selectors, animations, and local icon file paths.
- The current demo uses `docs/assets/styles/tokens.css`, `docs/assets/styles/site.css`, and `docs/assets/app.js`.

No external URLs are required for the demo to run.

